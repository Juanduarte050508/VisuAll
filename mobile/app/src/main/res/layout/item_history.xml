<?xml version="1.0" encoding="utf-8"?>
<!--
    Item do historico: hora e o texto.

    A etiqueta de origem ("LIBRAS" / "VOZ-TEXTO") saiu junto com o alinhamento
    esquerda-direita: cada folha mostra uma origem so, entao repetir isso linha
    a linha era ruido que o titulo da folha ja resolve.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/history_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="7dp">

    <TextView
        android:id="@+id/tv_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginBottom="4dp"
        android:textColor="@color/text_muted"
        android:textSize="11sp" />

    <TextView
        android:id="@+id/tv_word"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/vf_bg_phrase"
        android:paddingHorizontal="16dp"
        android:paddingVertical="11dp"
        android:textColor="@color/text_primary"
        android:textSize="16sp" />

</LinearLayout>
