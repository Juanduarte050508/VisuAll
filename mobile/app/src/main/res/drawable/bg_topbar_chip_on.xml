<?xml version="1.0" encoding="utf-8"?>
<!-- Mesma pílula do topo, no estado ligado: o dourado preenchido é o que
     diferencia "grade ligada" de "grade desligada" sem precisar de texto. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="14dp"/>
    <solid android:color="@color/gold_primary"/>
    <stroke android:width="1dp" android:color="@color/gold_light"/>
</shape>
